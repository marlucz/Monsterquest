const elements = {
  fetchBtn: document.getElementById('fetchBtn'),
  heroImages: document.querySelectorAll('.hero__image'),
  heroHeader: document.querySelector('.hero__header')
};

export default elements;
